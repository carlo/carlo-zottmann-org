doctype XML
urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9"
  - nodes_for_sitemap.each do |node|
    url
      loc = config['site_url'] + node.url
      - if node.data.date
        lastmod = node.data.date.iso8601
